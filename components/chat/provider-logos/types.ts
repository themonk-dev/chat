/**
 * Each mark is `aria-hidden`: it is normally rendered beside the provider's own
 * text label, which is what names the control. `ProviderMark` adds an
 * accessible name where the mark stands alone.
 */
export type ProviderLogoProps = {
  className?: string;
};
